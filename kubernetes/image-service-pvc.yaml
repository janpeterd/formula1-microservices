apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-service-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""  # Leave empty if the PV does not have a storage class
